```
It expands harnessing beyond PDF/OCR and covers the entire geotechCLI command surface: config, status, skill, bearing, liquefaction, classify, tunnel, vision, ingest, analyze, agent, chat, export, viz, plus proposed new command groups: tool, harness, geo, and qa.

The guidance is built around the same “humans steer, agents execute” harness-engineering idea: the repo, commands, tools, schemas, validators, traces, and feedback loops become the system that makes agents reliable. It also follows the agent-design principle that reliable systems should prefer simple, inspectable workflows and strong tools rather than adding unnecessary framework complexity.

I included a tool expansion roadmap because your current issue is real: the public geotechCLI docs list 49 bundled skills, but skills are higher-level workflows; the CLI needs many more small, callable, typed tools. The proposed roadmap targets roughly 80–120 tools for MVP and 180–250 tools for production, grouped by domain: core harness, evidence, files/data, units, geospatial, boreholes, lab, foundations, piles, retaining, slopes, liquefaction, tunnels, groundwater, monitoring, visualization, and export.

The implementation guide also includes:

- universal command lifecycle
- CommandSpec and ToolSpec schemas
- harness kernel architecture
- tool registry architecture
- guardrail runner
- trace recorder
- review gates
- command-by-command harness map
- proposed tool catalog
- new geotech tool / geotech harness / geotech geo / geotech qa command groups
- AGENTS.md structure
- Codex implementation prompt
- acceptance criteria
- citations and design references

For tool exposure, the guide uses MCP-style ideas: tools should be uniquely named, schema-described, discoverable, and protected with human-in-the-loop visibility for risky operations. For guardrails and traces, it references input/tool/output guardrail boundaries and run tracing concepts so Codex can implement a local equivalent inside geotechCLI.

For geotechnical interoperability, it also references AGS/AGSi export targets and CRS transformation guidance. AGS is used for geotechnical and geoenvironmental site-investigation data transfer, AGSi is for ground-model/interpreted data, and pyproj/PROJ should be the authoritative server-side coordinate transformation path.
  ```
